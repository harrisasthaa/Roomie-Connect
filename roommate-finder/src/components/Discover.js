import React from 'react';

export default function Discover() {

    const [users, setUsers] = useState(null);

    useEffect(() => {
        setUsers()
    })

    return
        <div>
            <div>
                {users.map((u) =>)}
            </div>
        </div>
}